import app from "../src/app";
import supertest from "supertest";

describe ('test GET /find/disciplines', () => {
    it.todo('Must return 200 and array with tests');
    it.todo('Must return 401 if no token')
})

describe ('test GET /find/teachers', () => {
    it.todo('Must return 200 and array with tests');
    it.todo('Must return 401 if no token')
})
